#include<stdio.h>
//Derived data type -> formed from the basic data types
void main() {
  typedef struct Box name;
    //int a[]={1,2,10,-1};
    name a[20]; //size fixed 0,1,..,19
    int n;
    char ch;
    printf("Enter the number of elements ");
    scanf("%d",&n);
    printf("Enter a charachter ");
    scanf("%c\n",&ch );
    if(st=="\n") scanf()

    /*
    for(int i=0;i<n;i++) {
      scanf("%f",&a[i]);
    }
    //a=a+1;

    printf("\nOutput is: ");
    for(int i=0;i<n;i++) { //i=4
      printf("%f ",a[i]);
    }*/
    //printf("%d",0[a]);
    //printf("%d %d",a,sizeof(a));
}
